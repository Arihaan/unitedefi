{
  "name": "1inch-fusion-celo-deployment",
  "version": "1.0.0",
  "description": "Complete 1inch Fusion+ deployment for Ethereum Sepolia and Celo Alfajores",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy-complete-stack.js",
    "deploy:sepolia": "hardhat run scripts/deploy-complete-stack.js --network sepolia",
    "deploy:celo": "hardhat run scripts/deploy-complete-stack.js --network celo",
    "verify": "hardhat verify"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.1.0",
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "ethers": "^6.15.0",
    "hardhat": "^2.19.0",
    "zksync-ethers": "^6.20.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}